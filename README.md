# Creature Creator Lab

A static TypeScript web app that generates whimsical creature profiles (goblins, gremlins, raccoons, trolls, ogres, pigeons, and more).

## Local development

```bash
npm install
npm run build
```

The built site is placed in `dist/` and is deployed by GitHub Actions via GitHub Pages.
